CREATE TABLE [dbo].[SkinPackageType] (
    [SkinPackageTypeId]   INT           IDENTITY (1, 1) NOT NULL,
    [SkinPackageTypeName] NVARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([SkinPackageTypeId] ASC)
);

